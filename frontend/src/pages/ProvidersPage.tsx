import { Alert, Stack, Typography } from "@mui/material";

export default function ProvidersPage() {
    return (
        <Stack spacing={2}>
            <Typography variant="h4">Providers</Typography>
            <Alert severity="info">Provider lookup UI will live here (coming soon).</Alert>
        </Stack>
    );
}
